name: test-runner

on: [push]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: test
        run: |
          whoami
          export KUBECONFIG=
          kubectl config view
          kubectl get pods -A